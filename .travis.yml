sudo: required

services:
  - docker

cache:
  directories:
  - $HOME/.m2
  - client/node_modules

matrix:
  include:
  - language: java
    script: cd server && mvn clean test

  - language: nodejs
    before_script:
      - cd client && npm install
    script:
      - cd client && npm run test